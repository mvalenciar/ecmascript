function newUser(user, age, country, id) {
  return {
    // user: user,
    // age: age,
    // country: country,
    // id: id,
    user,
    age,
    country,
    id,
  };
}

console.log(newUser('juan', 15, 'Colombia', 1));

const hello = 'Hello';
const world = 'World';

const epicPhrase = hello + ' ' + world;
console.log(epicPhrase);

// template literals
const templateLiterals = `${hello} ${world}`;
console.log(templateLiterals);

// Multi-line strings

const lorem = 'Esto es un string\n' + 'Esto es otra linea';
console.log(lorem);

const lorem2 = `Esto es un estrin
continuación del string`;
console.log(lorem2);
